import { StyleSheet } from "react-native";
import { colors } from "../../theme";

const ASPTextStyle = StyleSheet.create({
  text: {
    color: colors.white,
    fontSize: 20,
    fontFamily: "Avenir Next",
  },
});

export default ASPTextStyle;
